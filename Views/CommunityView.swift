import SwiftUI

struct CommunityView: View {
    var body: some View {
        VStack {
            Text("Comunidad")
                .font(.largeTitle)
                .padding()
            Text("Placeholder de comunidad")
                .foregroundColor(.gray)
        }
    }
}
