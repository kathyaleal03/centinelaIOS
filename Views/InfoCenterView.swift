import SwiftUI

struct InfoCenterView: View {
    var body: some View {
        VStack {
            Text("Información")
                .font(.largeTitle)
                .padding()
            Text("Centro de información placeholder")
                .foregroundColor(.gray)
        }
    }
}
