import SwiftUI

struct EmergencyButtonView: View {
    var body: some View {
        VStack {
            Text("Emergencia")
                .font(.largeTitle)
                .padding()
            Text("Botón de emergencia placeholder")
                .foregroundColor(.gray)
        }
    }
}
